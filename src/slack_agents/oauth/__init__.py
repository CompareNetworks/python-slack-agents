"""OAuth 2.1 support for MCP tool providers.

This package is loaded only when at least one `slack_agents.tools.mcp_http_oauth`
provider is configured. Importing it has no runtime effect on agents that don't
use OAuth-protected MCP servers.
"""
